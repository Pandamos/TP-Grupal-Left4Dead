#include "venom.h"

void venom::moverse(uint32_t nueva_posX, uint32_t nueva_posY) {
    //asumimos que se mueven a la misma velicudad que los infectados
    //if (nueva_pos_valida)
        //modifico pos actual
}

void venom::atacar(soldado& soldado) {
    //puede atacar lanzando bolas del gas corrosivo a larga distancia
    //o puede hacerlo desde cerquita

    //lo mas facil es hacerlo random
    //if (lanza_corta_distancia)    
        //soldado.daniar(DANIO_VENOM)

    //else
        // aca deberiamos lanzar la bola... nuevo objeto? y que esta se mueva a lo largo de la pantalla? 
        //casi como si fuese un nuevo enemigo...
}