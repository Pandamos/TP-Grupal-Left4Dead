#include "spear.h"

void spear::moverse(uint32_t nueva_posX, uint32_t nueva_posY) {
    //asumimos que se mueven a la misma velicudad que los infectados
    //if (nueva_pos_valida)
        //modifico pos actual
}

void spear::atacar(soldado& soldado) {
    //if (soldado_cerca)
        //soldado.daniar(DANIO_SPEAR)
}